import '../../../const/conts.dart';

class CustomLowerDesign extends StatelessWidget {
  const CustomLowerDesign({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      icLowerDesign,
      fit: BoxFit.contain,
    );
  }
}
