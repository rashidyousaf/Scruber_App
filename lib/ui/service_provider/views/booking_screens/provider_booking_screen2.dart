import 'package:flutter/material.dart';

class ProviderBookingScreen2 extends StatelessWidget {
  const ProviderBookingScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("provider b screen 2"),
      ),
    );
  }
}
