import 'package:flutter/material.dart';

class ProviderBookingScreen3 extends StatelessWidget {
  const ProviderBookingScreen3({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("provider b screen 3"),
      ),
    );
  }
}
